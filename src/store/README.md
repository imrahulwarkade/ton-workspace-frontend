# App-wide stores

Keep this folder empty unless a value is truly global and does not belong to a domain.

Environment toggle lives in `modules/catalog/store.ts`.
Favorites live in `modules/favorites/store.ts`.
