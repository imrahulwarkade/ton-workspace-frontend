# Catalog

Domain for ToneOp Workspace app URLs: seeded bookmarks plus admin-created staging/production links, search, and type filters.

Custom URLs persist in the browser (`toneop-workspace-catalog-apps`). Swap the store for the shared axios client when the Workspace API is ready.
